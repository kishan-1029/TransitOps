{
  "name": "transitops",
  "private": true,
  "scripts": {
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "db:up": "echo Neon only — set DATABASE_URL in server/.env",
    "db:setup": "npm run db:setup --prefix server",
    "setup": "npm install --prefix server && npm install --prefix client && npm run db:setup --prefix server",
    "build": "npm install --prefix client && npm run build --prefix client && npm install --prefix server && npm run build --prefix server",
    "start": "npm start --prefix server"
  },
  "engines": {
    "node": ">=20"
  }
}
